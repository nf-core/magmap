/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run nf-core/magmap -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '1.h'
    ]
}

params {
    config_profile_name        = 'Test profile for sourmash mixed with a local genome'
    config_profile_description = 'Minimal test dataset to check pipeline function with Sourmash subsetting enabled'

    // Input data
    input               = params.pipelines_testdata_base_path + 'magmap/samplesheets/sourmash_mix_samples.csv'
    indexes             = params.pipelines_testdata_base_path + 'magmap/testdata/sourmash_24genomes.k31.index.sbt.zip'
    genomeinfo          = params.pipelines_testdata_base_path + 'magmap/testdata/e_coli_genome.csv'
    sourmash_ksize      = 31
    features            = 'tRNA,rRNA'
    gtdb_metadata       = "${params.pipelines_testdata_base_path}magmap/testdata/ar53_metadata.tsv,${params.pipelines_testdata_base_path}magmap/testdata/bac120_metadata.tsv"
    checkm_metadata     = params.pipelines_testdata_base_path + 'magmap/testdata/ecoli_genome.checkm2.quality_report.tsv'
    gtdbtk_metadata     = params.pipelines_testdata_base_path + 'magmap/testdata/ecoli_genome.gtdbtk.bac120.summary.tsv'

    // Sourmash option
    skip_sourmash       = false

}
