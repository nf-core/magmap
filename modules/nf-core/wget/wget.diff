Changes in component 'nf-core/wget'
'modules/nf-core/wget/meta.yml' is unchanged
Changes in 'wget/main.nf':
--- modules/nf-core/wget/main.nf
+++ modules/nf-core/wget/main.nf
@@ -8,10 +8,10 @@
         : 'community.wave.seqera.io/library/wget:1.21.4--8b0fcde81c17be5e'}"
 
     input:
-    tuple val(meta), val(url), val(suffix)
+    tuple val(meta), val(url)
 
     output:
-    tuple val(meta), path("${prefix}.${suffix}"), emit: outfile
+    tuple val(meta), path("${url.substring(url.lastIndexOf('/')+1, url.length())}"), emit: outfile
     tuple val("${task.process}"), val('wget'), eval('wget --version | head -1 | cut -d " " -f 3'), emit: versions_wget, topic: versions
 
     when:
@@ -19,18 +19,14 @@
 
     script:
     def args = task.ext.args ?: ''
-    prefix = task.ext.prefix ?: "${meta.id}"
     """
     wget \\
-        -O - \\
         ${args} \\
-        ${url} \\
-        > ${prefix}.${suffix}
+        ${url}
     """
 
     stub:
-    prefix = task.ext.prefix ?: "${meta.id}"
     """
-    touch ${prefix}.${suffix}
+    touch ${url.substring(url.lastIndexOf('/')+1, url.length())}
     """
 }

'modules/nf-core/wget/environment.yml' is unchanged
'modules/nf-core/wget/tests/nextflow.config' is unchanged
'modules/nf-core/wget/tests/main.nf.test.snap' is unchanged
'modules/nf-core/wget/tests/main.nf.test' is unchanged
************************************************************
